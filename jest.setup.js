// jest.setup.js
import '@testing-library/jest-dom';

// You can add other global setup here, for example:
// - Mocking global objects (localStorage, fetch, etc.)
// - Setting up a common state for tests

// Example: Mock Next.js router for tests that use it
// jest.mock('next/router', () => require('next-router-mock'));

// Example: Mock environment variables if needed
// process.env.NEXT_PUBLIC_SOME_VAR = 'test_value';
